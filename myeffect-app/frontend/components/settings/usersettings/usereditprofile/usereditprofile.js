import React, { Component } from 'react'

export default class usereditprofile extends Component {
    render() {
        return (
            <div>
                This is the users profile edit section of settings
            </div>
        )
    }
}
